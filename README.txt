To install and run the app:

1.-Git clone the project.
2.-Go to https://opencv.org/releases/page/2/ and download OpenCV – 4.5.1 and extract it.
3.-Copy the opencv extracted folder into src/main/resources folder in the server (backend) folder of the project.
4.-You can run the backend by just running it as a Spring Project, you can import the server folder as a project from any IDE you want (Eclipse, IntellIJ, etc)
5.-To run the frontend, open a command prompt inside the /ui folder and enter the following command: yarn dev.
